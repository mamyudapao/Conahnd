<div class="gravatar">
  <%= link_to gravatar_for(current_user, size: 200), current_user %>
</div>
<h1><%= current_user.name %></h1>
<span><%= link_to "プロフィールを確認する", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "post") %></span>
