<% provide(:title, 'All users') %>
<div class="users-field container">
  <h1>All users</h1>

  <%= will_paginate %>

  <ul class="users">
    <% @users.each do |user| %>
    <%= render user %>
    <% end %>
  </ul>

  <%= will_paginate %>
</div>
